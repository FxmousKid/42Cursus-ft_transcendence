ELASTIC_USERNAME={{ with secret "kv/data/elk/elasticsearch" }}{{ .Data.data.username }}{{ end }}
ELASTIC_PASSWORD={{ with secret "kv/data/elk/elasticsearch" }}{{ .Data.data.password }}{{ end }}
LOGSTASH_USERNAME={{ with secret "kv/data/elk/logstash" }}{{ .Data.data.username }}{{ end }}
LOGSTASH_PASSWORD={{ with secret "kv/data/elk/logstash" }}{{ .Data.data.password }}{{ end }}
KIBANA_USERNAME={{ with secret "kv/data/elk/kibana" }}{{ .Data.data.username }}{{ end }}
KIBANA_PASSWORD={{ with secret "kv/data/elk/kibana" }}{{ .Data.data.password }}{{ end }}


# Frontend configuration
VITE_APP_NAME=Transcendence
FRONTEND_PORT=5173
FRONTEND_URL=http://localhost:5173

# Backend configuration
BACKEND_PORT=3000
BACKEND_URL=http://localhost:3000

# Pour l'accès aux API (utilisée par le frontend)
# En local
API_URL=http://localhost:3001
# En Docker (le nom du service dans docker-compose)
VITE_API_URL=http://backend:3001
VITE_APP_NAME=Transcendence 

# Server configuration
PORT=3000
HOST=0.0.0.0

# Database Configuration
DATABASE_URL="sqlite:./db.sqlite"

# JWT Configuration
JWT_SECRET="your-super-secret-key-here"
JWT_EXPIRATION="1d"

# CORS Configuration
CORS_ORIGIN="http://localhost:5173"

# API Configuration
API_PREFIX="/api"
SWAGGER_PATH="/docs" 

# Version of Elastic products
STACK_VERSION=8.7.1

# Set the cluster name for elasticsearch
CLUSTER_NAME=docker-cluster

# Set to 'basic' or 'trial' to automatically start the 30-day trial of es-cloud
LICENSE=basic
#LICENSE=trial

# Port to expose Elasticsearch HTTP API to the host
ES_PORT=9200

# Port to expose Kibana to the host
KIBANA_PORT=5601

# Increase or decrease based on the available host memory (in bytes)
ES_MEM_LIMIT=1073741824
KB_MEM_LIMIT=1073741824
LS_MEM_LIMIT=1073741824

# SAMPLE Predefined Key only to be used in POC environments
ENCRYPTION_KEY=c34d38b3a14956121ff2170e5030b471551370178f43e5626eec58b04a30fae2
