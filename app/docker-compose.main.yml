

# include the compsoe of every provided type of service

include:
  - ./core/docker-compose.core.yml                    # Core services
  - ./infra/vault/docker-compose.vault.yml            # Hashicorp Vault services
  - ./infra/logging/docker-compose.logging.yml        # ELK stack services
  - ./infra/monitoring/docker-compose.monitoring.yml  # Monitoring services


# Global network
networks:
  transcendence-network:
    driver: bridge

volumes:
  prometheus_data:
  grafana_data:
  escerts:
  esdata01:
  filebeatdata01:
  kibanadata:
